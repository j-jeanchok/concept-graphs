defaults:
  - base
  - base_mapping
  - realsense
  # - record3d 
  # - replica
  - sam
  - classes
  - logging_level
  - _self_
  

# hydra:
#   verbose : true

detections_exp_suffix: s_detections_stride1_1 # s_detections_stride1_3 # s_detections_stride50_2 # exp_s_detections_stride50_yes_bg_44_mr
make_edges: !!bool True
stride: 1
exp_suffix: s_mapping_stride1_1
save_video: !!bool True
save_objects_all_frames: !!bool True
vis_render: !!bool False

# log_level: INFO
downsample_voxel_size: 0.01
obj_pcd_max_points: 5000
run_filter_final_frame: !!bool True
run_merge_final_frame: !!bool True

denoise_interval: 5           # Run DBSCAN every k frame. This operation is heavy
filter_interval: 5            # Filter objects that have too few associations or are too small
merge_interval: 5             # Merge objects based on geometric and semantic similarity

hydra:
  verbose: true
  # mode: MULTIRUN
  # sweeper:
  #   params:
  #     scene_id: room0, office0 # 